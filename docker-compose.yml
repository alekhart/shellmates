services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: shellmates
      POSTGRES_USER: shellmates
      POSTGRES_PASSWORD: shellmates_dev
    ports:
      - '5433:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
